services:
  - type: web
    name: attendance-app
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn Attendance_Module.wsgi:application"
    envVars:
      DJANGO_SETTINGS_MODULE: Attendance_Module.settings
