[build]
builder = "NIXPACKS"
python_version = "3.10"

[deploy]
start = "gunicorn Attendance_Module.wsgi:application"
restart_policy = "on_failure"

[variables]
# Declare environment variables so Railpack recognizes them as secrets
MONGO_URI = "secret"
SECRET_KEY = "secret"
DEBUG = "false"
